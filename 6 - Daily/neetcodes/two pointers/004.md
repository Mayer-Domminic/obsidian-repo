---
tags:
  - neetcode
  - review
  - Medium
  - twopointers
date: 2/23/2025
---

# Problem Title

## Problem Description
You are given an integer array heights where heights[i] represents the height of the ith bar.

You may choose any two bars to form a container. Return the maximum amount of water a container can store.

## Approach
I completely got this one, no hints no nothing. First super successful medium. LFG

## Code
```python
class Solution:
    def maxArea(self, heights: List[int]) -> int:
        length = len(heights)
        i = 0
        output = 0
        left = 0
        
        for i in range(length):
            right = length-1
            while i < right:
                distance = right - i
                l = heights[i]
                r = heights[right]
                minimum = min(l, r)
                t = (minimum)*(distance)
                print(l, r, t, output, distance)

                if t > output:
                    output = t
                
                right -= 1
        return output
